export interface Topusers { //Se encarga de trabajar de forma fuertemente tipadas lo mismo ocurre con "palabras.ts"
	_id:string;
	nombre:string;
	tiempo:number;
	palabra:string;
	nivel:string;
	foto:string;
}